#include <stdio.h>
/**
* main - a special & first function that gets excueted
* int - specifies the return type as an intger
* viod - indicates that main takes no parameters
* puts - a function used to output a string
* Return: zero, the program executed successfully with no errors
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
