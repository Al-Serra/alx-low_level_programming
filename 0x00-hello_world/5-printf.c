#include <stdio.h>
/**
 * main - the first function that gets executed, the entry point for the pogram
 * printf - a function that allows you to control the formatting of the output
 * Return: zero, the program executed successfully without any errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
